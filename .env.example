# PLANTILLA SIN SECRETOS — copia a .env (ese NUNCA se sube a git)
# TEMPLATE WITHOUT SECRETS — copy to .env (that one is NEVER committed)
WORKAPP_LANG=es
WORKAPP_LOCATION=Colombia
CACHE_TTL_HOURS=24
# Genera uno propio: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET=PON-AQUI-UN-VALOR-LARGO-ALEATORIO
PORT=5000
# Opcional / Optional (tus keys van en .env local, jamás aquí):
# SUPABASE_URL=
# SUPABASE_KEY=
# BLOCKED_COMPANIES=empresa-fantasma-1,empresa-fantasma-2
